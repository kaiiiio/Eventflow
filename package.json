{
  "name": "eventflow",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "libs/*"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean",
    "dev:orchestrator": "cd apps/orchestrator && npm run dev",
    "dev:worker-template": "cd apps/worker-template && npm run dev"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}

